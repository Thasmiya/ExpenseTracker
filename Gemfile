source 'https://rubygems.org'
ruby '3.1.0' # adjust to your ruby version

gem 'rails', '~> 7.0.0'
gem 'pg', '~> 1.2'                 # PostgreSQL adapter
gem 'puma', '~> 5.0'
gem 'bcrypt', '~> 3.1.18'          # has_secure_password
gem 'jwt', '~> 2.3'                # JWT handling
gem 'rack-cors'                    # for cross origin (if needed)
gem 'jbuilder', '~> 2.7'

group :development, :test do
  gem 'pry'
  gem 'rspec-rails'                # optional testing
end
